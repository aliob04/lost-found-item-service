name: Deploy item-service

on:
  push:
    branches: ["master"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy lost-found-item-service \
            --source . \
            --region us-central1 \
            --allow-unauthenticated \
            --add-cloudsql-instances swe455hw2:us-central1:lost-found-postgres \
            --set-env-vars DATABASE_URL='postgresql://lostfounduser:LostFound12345!@/lostfound?host=/cloudsql/swe455hw2:us-central1:lost-found-postgres',ITEM_CREATED_TOPIC=item-created \
            --quiet
